{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install All Recommended Extensions",
      "type": "shell",
      "command": "foreach ($ext in (Get-Content -Raw .vscode/mandatoryExtension.json | ConvertFrom-Json)) { Write-Host Installing $ext; code --install-extension $ext; }",
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}
